def main(private field candidateId, field validCandidateId, field totalCandidates) -> field {
    assert(candidateId > 0);
    assert(candidateId <= totalCandidates);
    assert(candidateId == validCandidateId);
    return 1;
}
